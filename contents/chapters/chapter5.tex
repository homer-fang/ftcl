\chapter{实验评估}

\section{实验目标（Evaluation Goals）}
本章实验回答三个问题。第一，FTCL 的语言运行时是否保持了预期语义？第二，CPU 与 CUDA 几何后端是否能产生等价结果？第三，从 FTCL 脚本用户的角度看，CUDA 在什么规模下能够带来\textbf{端到端加速比}（end-to-end speedup）？

项目的实验数据、CSV 文件和图表生成脚本都位于 \texttt{docs} 目录下。测量内容包括语义通过率、解析器后端计时、线程通道延迟、帧时间分布、CPU/CUDA 几何加速比、吞吐量、数值误差、盈亏平衡点、消融数据与并发几何性能。这些指标共同覆盖正确性、性能与可扩展性三个层面。

\section{实验方法（Experimental Methodology）}
除非特别说明，本文所有性能结果都被视为\textbf{端到端系统测量}（end-to-end system measurements）。一次几何命令测量包含 FTCL 命令分发、参数解析、UVec 句柄查找、设备同步、输出 UVec 分配、后端执行，以及 CUDA kernel 启动后的同步。它不是纯 CUDA kernel time。本文有意采用这种测量方式，因为论文评价的是脚本用户实际看到的系统性能。

\textbf{基准流水线}（benchmark pipeline）遵循以下原则：

\begin{enumerate}
  \item \textbf{使用 Release 构建。} Debug 构建不进入性能讨论，因为解释器分发与 STL 容器对优化级别非常敏感。
  \item \textbf{区分预热与测量。} 解析器后端计时与几何计时脚本在可支持的情况下会先执行预热运行，再记录样本。
  \item \textbf{优先使用中位数或分位数。} 延迟分布使用 P50、P95 和 P99 描述；几何计时在有多个样本时使用具有代表性的命令延迟中位数。
  \item \textbf{单独看待读回成本。} 部分实验将结果保留在 UVec 句柄中，消融实验则显式测量将大规模 UVec 输出转回脚本列表的成本。
  \item \textbf{先比较正确性，再解释速度。} 只有 CPU/GPU 等价性测试在数值容忍范围内通过时，CUDA 加速比才有意义。
\end{enumerate}

主要派生指标定义如下：
\[
  \mathrm{speedup} = \frac{T_{\mathrm{CPU}}}{T_{\mathrm{CUDA}}},
  \quad
  \mathrm{throughput} = \frac{\mathrm{work\ items}}{T},
  \quad
  \mathrm{work\ items} = N \times Q.
\]
对于 distance matrix、nearest point 和 range-count 工作负载，\(N\) 表示数据点数量，\(Q\) 表示查询点或查询区域数量。

\subsection{CPU 基线定义（CPU Baseline Definition）}
本文的 CPU 基线是 FTCL CPU 几何后端，而不是手工优化的并行 CPU 库。这个区别很重要：实验回答的是脚本用户会问的问题——同一个 FTCL 命令在 CPU 设备与 CUDA 设备上执行时，端到端提升是多少。

因此，CPU 行与 CUDA 行尽量共享相同的脚本层命令路径。主性能实验中，输入会在计时前预先构造为 UVec 句柄；计时区间包括命令分发、句柄查找、输出 UVec 分配、后端执行与必要同步。CPU 行使用 C++ 标量循环处理驻留在 CPU 的 UVec 数据；CUDA 行使用 CUDA 设备缓冲区与 kernel，且当前 CUDA 辅助函数在 kernel 启动后调用 \texttt{cudaDeviceSynchronize}，保证命令返回时后端工作已完成。

该基线对评价 FTCL 作为异构脚本系统是公平的，但不能被解释为对抗高度优化的全核 CPU 几何库。实验服务器拥有 128 个逻辑 CPU 线程，而当前 CPU 几何后端故意保持简单。因此，报告的加速比衡量的是：在相同命令接口下，将 FTCL 后端从标量 CPU 实现迁移到 CUDA 实现后获得的收益。未来加入全核 CPU 后端将是有价值的补充基线。

\begin{table}[H]
\centering
\caption{实验环境信息（Experimental environment for reproducibility）}
\label{tab:environment}
\begin{tabularx}{\textwidth}{lX}
\toprule
Item & 记录值 \\
\midrule
操作系统 & 用于构建、测试、CUDA 执行与图表生成的 WSL Ubuntu 环境 \\
CPU 型号 & Intel Xeon Platinum 8358，2.60 GHz \\
CPU 拓扑 & 2 路插槽，每路 32 核，每核 2 硬件线程，共 128 逻辑 CPU \\
CPU 频率 & 最低 800 MHz，最高 3.40 GHz（\texttt{lscpu} 报告值） \\
NUMA 布局 & 2 个 NUMA 节点；节点 0 含 CPU 0--31 与 64--95，节点 1 含 CPU 32--63 与 96--127 \\
主存 & 总内存 1.0 TiB；记录环境时约 623 GiB 可用 \\
GPU 型号 & 8 张 NVIDIA A800-SXM4-80GB \\
GPU 显存 & 每张 81920 MiB，合计约 640 GiB 设备内存 \\
GPU 驱动 & NVIDIA driver 530.30.02 \\
CUDA 计算能力 & 每张 A800 GPU 为 8.0 \\
构建系统 & CMake Release 配置 \\
C++ 标准 & C++20，由 \texttt{FTCL\_CXX\_STANDARD=20} 指定 \\
编译器 & WSL 内可用的 GCC 或 Clang \\
CUDA 选项 & \texttt{FTCL\_ENABLE\_CUDA=ON}，用于 CUDA 几何实验 \\
CPU 构建路径 & \texttt{build} 或 \texttt{/tmp/ftcl-build-geometry-cpu} \\
CUDA 构建路径 & \texttt{/tmp/ftcl-build-geometry-cuda} \\
基准数据 & \texttt{docs/data/benchmarks} 与 \texttt{docs/data/geometry} 下的 CSV 文件 \\
生成图表 & \texttt{docs/figures} 与 \texttt{contents/figures} 下的 SVG/PNG 图 \\
重要说明 & 可用 GPU 显存随时间变化，因服务器可能被其他用户或作业共享 \\
\bottomrule
\end{tabularx}
\end{table}

表 \ref{tab:environment} 是环境记录，而不是硬件无关结论。本文所有性能数据都应理解为在该多 GPU 服务器上的实测结果。服务器的 CPU 核心数也说明 CPU 基线并非来自弱硬件；真正的实验边界在于当前 CPU 后端采用标量实现，而非全并行 CPU 实现。

\section{语义回归测试（Semantic Regression Tests）}
\textbf{语义回归测试}（semantic regression tests）覆盖核心命令、集合、控制流、表达式与线程。图 \ref{fig:semantic-pass} 显示当前测量的测试套件达到 100\% 通过率。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{semantic-pass-rate.png}
  \caption{测试套件语义通过率（Semantic pass rate by test suite）}
  \label{fig:semantic-pass}
\end{figure}

这个结果并不表示 FTCL 已经完整实现 Tcl。它表示本文选定的 Tcl subset 已经形成稳定 regression baseline。对于 parser 和 expression 这类容易引入细微语义变化的模块，这一点尤其重要。

\section{解析器后端计时（Parser Backend Timing）}
图 \ref{fig:parser-backend} 比较传统解析器与词法流解析器在 Tcl 子集基准上的耗时。词法流路径并不一定更快，因为它增加了词法分析阶段并保存 token 元数据。其价值主要在架构层面：更好的诊断、更清晰的命令边界，以及通向 AST 与字节码执行的路径。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.80\textwidth]{parser-backend-timing.png}
  \caption{Parser backend 耗时对比（Timing comparison between parser backends）}
  \label{fig:parser-backend}
\end{figure}

直接结论：token-stream 解析器的短期意义不是速度，而是可维护性、错误定位与后续编译路径。

\section{并发与运行时平滑性（Concurrency and Runtime Smoothness）}
线程通道延迟与帧时间分布如图 \ref{fig:channel-latency} 与图 \ref{fig:frame-time} 所示。通道实验通过乒乓往返估计单向延迟；帧时间实验测量一个非交互式类游戏的负载。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{channel-latency.png}
  \caption{线程通道单向延迟分布（Thread-channel one-way latency distribution）}
  \label{fig:channel-latency}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{frame-time.png}
  \caption{类游戏 workload 的帧耗时分布（Frame-time distribution）}
  \label{fig:frame-time}
\end{figure}

这些实验说明 FTCL 可以支持简单的并发脚本与交互式演示。相比平均值，P50、P95 和 P99 等尾延迟指标更能反映脚本系统的实际交互体验。

\section{CPU/GPU 等价性验证（CPU/GPU Equivalence）}
对 CUDA 后端而言，\textbf{正确性}比速度更重要。\textbf{等价性测试}（equivalence tests）让同一几何工作负载同时在 CPU 与 CUDA 上运行，比较返回的 UVec 值，并允许较小的浮点容差。图 \ref{fig:error-dist} 展示数值误差分布。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.70\textwidth]{geometry-error-distribution.png}
  \caption{CPU 与 CUDA 几何结果的数值误差分布（Numerical error distribution）}
  \label{fig:error-dist}
\end{figure}

生成数据中的观测误差接近零。这支持了本文的基本正确性主张：在测试覆盖到的工作负载上，CUDA 实现保持了 CPU 几何语义。

\section{几何性能扩展（Geometry Performance Scaling）}
性能实验聚焦三个代表性算法：距离矩阵、最近点与圆形范围计数。它们的共同点是存在大量独立工作项。表 \ref{tab:geom-speedup} 根据保存的性能 CSV 总结端到端加速比。

\textbf{单 GPU 性能扩展实验}使用一个 FTCL 解释器和一个 CUDA 设备。每个规模下，基准会在计时前构造 CPU 与 CUDA UVec 输入，执行可选的非计时预热（warmup），并报告命令耗时的中位数。这意味着点列表解析成本被排除在主计时之外，而输出分配与后端同步仍包含在内。实验目标是测量预构建 UVec 句柄在 realistic 脚本流水线中的使用成本。

\begin{table}[H]
\centering
\caption{代表性几何算法的端到端 CPU/CUDA 加速比}
\label{tab:geom-speedup}
\begin{tabular}{lrrr}
\toprule
算法 & N,Q & 工作项 & 加速比 \\
\midrule
距离矩阵 & 2048, 256 & 524288 & 6.77x \\
最近点 & 2048, 256 & 524288 & 2.05x \\
圆形范围计数 & 2048, 256 & 524288 & 1.41x \\
距离矩阵 & 8192, 1024 & 8388608 & 51.90x \\
最近点 & 8192, 1024 & 8388608 & 8.39x \\
圆形范围计数 & 8192, 1024 & 8388608 & 6.50x \\
距离矩阵 & 32768, 2048 & 67108864 & 224.00x \\
最近点 & 32768, 2048 & 67108864 & 16.55x \\
圆形范围计数 & 32768, 2048 & 67108864 & 13.13x \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.82\textwidth]{geometry-speedup.png}
  \caption{批量几何算法的 CPU/CUDA speedup}
  \label{fig:geom-speedup}
\end{figure}

图 \ref{fig:execution-bars} 与图 \ref{fig:throughput} 展示执行时间与吞吐量。距离矩阵获得最高加速比，因为其工作项简单且彼此独立。最近点与范围计数同样受益于 CUDA，但每个查询需要更多内存读取，因此扩展模式不完全相同。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.82\textwidth]{geometry-execution-bars.png}
  \caption{最大测量规模下的执行时间对比（Execution-time comparison）}
  \label{fig:execution-bars}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.82\textwidth]{geometry-throughput.png}
  \caption{几何算法吞吐量曲线（Throughput curve）}
  \label{fig:throughput}
\end{figure}

直接结论：工作项足够多且输出紧凑时，CUDA 后端的优势更稳定；输出规模过大时，分配与内存带宽会成为瓶颈。

\section{多 GPU 扩展（Multi-GPU Scaling）}
实验服务器包含 8 张 A800 GPU，因此本文进一步测量 FTCL 是否能把多个 CUDA 设备暴露给脚本层几何工作负载。\textbf{多 GPU 基准}（multi-GPU benchmark）使用弱扩展（weak scaling）：每张 GPU 接收相同数量的查询点，总查询数随 GPU 数量增长。每个 worker 拥有独立 FTCL interpreter，在不同 CUDA device 上创建 UVec inputs，并分别在 \texttt{cuda:0}、\texttt{cuda:1}、\ldots、\texttt{cuda:7} 上启动相同 geometry command。报告的 speedup 是相对于 1 GPU 的 throughput speedup。

\textbf{基准运行}时需要让所有设备可见，例如：

\begin{lstlisting}[language=bash]
CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6,7 \
FTCL_MULTI_GPU_MODE=paper \
FTCL_MULTI_GPU_SCALING=weak \
test/bench_geometry_multi_gpu
\end{lstlisting}

记录运行中，\(N=32768\)，每张 GPU 接收 \(Q=4096\) 个查询对象，每行报告三轮中位数。因此 1、2、4、8 张 GPU 对应的总查询数分别为 \(4096\)、\(8192\)、\(16384\) 与 \(32768\)。计时器测量从同时释放所有 worker 线程到所有 worker 获得输出 UVec 句柄的 wall-clock 区间。长度检查与输出销毁在计时区间之后执行，因此计时聚焦命令分发、UVec 句柄查找、输出分配、CUDA 启动、后端同步与结果句柄创建。

该实验还暴露了一个重要的运行时瓶颈。原始 UVec 命令管理器使用全局 mutex 保护整个句柄表，并在持锁状态下执行几何命令，导致彼此独立的 CUDA 命令在脚本运行时层被串行化。实现后来改为：全局 mutex 只保护句柄表查找，每个 UVec 句柄再拥有独立条目 mutex，使不同 GPU 上的独立 UVec 句柄可以并发执行。

\begin{table}[H]
\centering
\caption{多 GPU 几何命令 weak-scaling speedup}
\label{tab:multi-gpu-speedup}
\begin{tabular}{lrrrr}
\toprule
算法 & 1 GPU & 2 GPU & 4 GPU & 8 GPU \\
\midrule
距离矩阵 & 1.00x & 0.96x & 0.97x & 1.12x \\
最近点 & 1.00x & 2.00x & 3.99x & 3.74x \\
圆形范围计数 & 1.00x & 1.98x & 3.97x & 7.90x \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.82\textwidth]{geometry-multi-gpu-scaling.png}
  \caption{1、2、4、8 张 A800 GPU 的 weak-scaling speedup}
  \label{fig:multi-gpu-scaling}
\end{figure}

\subsection{多 GPU 弱扩展模型（Weak-scaling Model）}
为了解释表 \ref{tab:multi-gpu-speedup} 中三类算法差异较大的扩展结果，需要先明确本实验采用的是弱扩展（weak scaling），而不是强扩展（strong scaling）。强扩展固定总工作量，观察更多 GPU 是否能更快完成同一份任务；弱扩展则固定每张 GPU 的局部工作量，让总工作量随 GPU 数量增加。本文的 multi-GPU benchmark 属于后一种情形：每张 GPU 都处理规模相同的一份 geometry workload。

设 \(g\) 为 GPU 数量，\(W\) 为单张 GPU 上的局部工作项数，\(T_1\) 为 1 张 GPU 处理 1 份任务的 wall-clock time，\(T_g\) 为 \(g\) 张 GPU 同时处理 \(g\) 份任务的 wall-clock time。则 1 GPU 与 \(g\) GPU 的吞吐量分别为
\[
  \mathrm{Throughput}(1)=\frac{W}{T_1},
  \qquad
  \mathrm{Throughput}(g)=\frac{gW}{T_g}.
\]
因此 weak-scaling throughput speedup 定义为
\[
  S_{\mathrm{weak}}(g)
  =
  \frac{\mathrm{Throughput}(g)}{\mathrm{Throughput}(1)}
  =
  g\frac{T_1}{T_g}.
\]
这个公式给出了一个直接判据：如果 \(T_g \approx T_1\)，则 \(S_{\mathrm{weak}}(g)\approx g\)，说明系统接近线性扩展；如果 \(T_g\approx 2T_1\)，则 8 GPU 时 \(S_{\mathrm{weak}}(8)\approx 4\)；如果 \(T_g\approx 8T_1\)，则 \(S_{\mathrm{weak}}(8)\approx 1\)，说明增加 GPU 主要增加了总工作量，却没有提高整体吞吐效率。

更真实的多 GPU wall-clock time 不能只写成 CUDA kernel time，而应拆分为
\[
  T_g
  =
  \max_{1\le i\le g} T_{\mathrm{local},i}
  +
  T_{\mathrm{shared}}(g)
  +
  T_{\mathrm{serial}}(g).
\]
其中，\(T_{\mathrm{local},i}\) 表示第 \(i\) 张 GPU 的本地执行时间，包括 kernel computation、本地 output write 和 device-side allocation 等；\(T_{\mathrm{shared}}(g)\) 表示随 GPU 数量增加而出现的共享资源竞争，例如 CUDA runtime、host allocator、UVec manager、NUMA/PCIe/driver contention；\(T_{\mathrm{serial}}(g)\) 表示真正串行或近似串行的部分，例如全局锁、统一调度、结果句柄管理、join 和 handle-table 操作。理想情况下，
\[
  T_{\mathrm{shared}}(g)\approx 0,
  \qquad
  T_{\mathrm{serial}}(g)\approx 0,
\]
于是 \(T_g\approx T_1\)，从而 \(S_{\mathrm{weak}}(g)\approx g\)。反之，如果共享路径或串行路径被大输出放大，使得 \(T_g\approx gT_1\)，那么
\[
  S_{\mathrm{weak}}(g)
  =
  g\frac{T_1}{gT_1}
  =
  1.
\]
这正是距离矩阵在 8 GPU 下接近 1 倍 throughput speedup 的数学含义。

三个算法的核心差异不仅是计算复杂度，更是输出复杂度。它们的主要计算量都可以写成 \(O(NQ)\)，但输出规模不同，如表 \ref{tab:multi-gpu-output-model} 所示。

\begin{table}[H]
\centering
\caption{多 GPU 扩展中的计算复杂度与输出复杂度}
\label{tab:multi-gpu-output-model}
\begin{tabular}{lccc}
\toprule
算法 & 计算量 & 输出量 & 多 GPU 表现解释 \\
\midrule
圆形范围计数 & \(O(NQ)\) & \(O(Q)\) & 输出紧凑，共享瓶颈弱，接近线性扩展 \\
最近点 & \(O(NQ)\) & \(O(Q)\) & 输出紧凑，但 8 GPU 时调度与同步开销上升 \\
距离矩阵 & \(O(NQ)\) & \(O(NQ)\) & 完整物化大矩阵，输出路径成为主瓶颈 \\
\bottomrule
\end{tabular}
\end{table}

圆形范围计数中，每个 query 只输出一个 count，因此 \(O_{\mathrm{range}}=O(Q)\)。输出很小，UVec output allocation、global memory write 和 host-side result management 都相对轻量，所以在 8 GPU 时 \(T_8\approx T_1\)，实验结果达到 \(7.90\times\)，接近理想线性扩展。

最近点查询同样每个 query 只输出一个最近点结果，输出规模为 \(O_{\mathrm{nearest}}=O(Q)\)。因此 1、2、4 GPU 时扩展较好；但到 8 GPU 时，worker scheduling、CUDA synchronization、output allocation 与 runtime contention 开始变得明显。由表 \ref{tab:multi-gpu-speedup} 的 \(S_{\mathrm{weak}}(8)=3.74\) 可反推
\[
  T_8
  =
  8\frac{T_1}{S_{\mathrm{weak}}(8)}
  \approx
  2.14T_1,
\]
因此吞吐加速约为 4 倍，而不是 8 倍。

距离矩阵的情况不同。它不仅计算所有 point pairs，还要完整输出所有距离，因此输出规模为 \(O_{\mathrm{dist}}=O(NQ)\)。在本文 multi-GPU 实验设置中，每张 GPU 需要输出
\[
  32768\times 4096 = 134217728
\]
个距离值。如果每个距离使用 4 字节 float 表示，则单张 GPU 的输出约为
\[
  134217728\times 4 \approx 512\ \mathrm{MiB},
\]
8 张 GPU 的总输出约为 \(4\ \mathrm{GiB}\)。因此距离矩阵的问题不是缺少并行计算，而是当前实现会完整物化一个巨大输出矩阵，并放大 output UVec allocation、global memory write、CUDA runtime synchronization、handle management 和 result object lifetime management 等成本。由 \(S_{\mathrm{weak}}(8)=1.12\) 可反推
\[
  T_8
  =
  8\frac{T_1}{1.12}
  \approx
  7.14T_1,
\]
因此 throughput speedup 接近 1 倍。

为了用一个更紧凑的量描述这种现象，可引入\textbf{有效瓶颈比例}（effective bottleneck ratio）\(\rho\)，并用简化模型
\[
  T_g = T_1\left((1-\rho)+\rho g\right)
\]
近似多 GPU wall-clock time。对应的 weak-scaling speedup 为
\[
  S_{\mathrm{weak}}(g)
  =
  \frac{g}{(1-\rho)+\rho g}
  =
  \frac{g}{1+\rho(g-1)}.
\]
这里的 \(\rho\) 不是严格意义上的 serial code fraction，而是在该实验设置下没有随 GPU 数量并行扩展、反而表现为共享或串行瓶颈的有效比例。由 \(g=8\) 的实测 speedup 反推
\[
  \rho
  =
  \frac{g/S_{\mathrm{weak}}(g)-1}{g-1},
\]
可得到表 \ref{tab:effective-bottleneck-ratio}。

\begin{table}[H]
\centering
\caption{由 8 GPU weak-scaling 结果反推的有效瓶颈比例}
\label{tab:effective-bottleneck-ratio}
\begin{tabular}{lcc}
\toprule
算法 & \(S_{\mathrm{weak}}(8)\) & \(\rho\) \\
\midrule
圆形范围计数 & \(7.90\times\) & \(0.18\%\) \\
最近点 & \(3.74\times\) & \(16.3\%\) \\
距离矩阵 & \(1.12\times\) & \(87.8\%\) \\
\bottomrule
\end{tabular}
\end{table}

这个模型解释了表 \ref{tab:multi-gpu-speedup} 的主要现象：圆形范围计数几乎没有有效瓶颈，所以接近 8 倍；最近点存在中等运行时瓶颈，所以约 4 倍；距离矩阵大部分时间被输出路径与共享瓶颈主导，所以接近 1 倍。换言之，多 GPU 扩展不仅取决于计算复杂度，还取决于输出复杂度、数据驻留策略、运行时锁粒度与同步路径。

直接结论：圆形范围计数接近线性多 GPU 吞吐量扩展，而距离矩阵暴露出输出主导型瓶颈。

结果说明多 GPU 执行有价值，但强烈依赖算法形态。圆形范围计数几乎线性扩展到 8 张 GPU，因为每个查询只产生一个紧凑结果，且工作模式规则。最近点扩展到 4 张 GPU 表现良好，但到 8 张 GPU 后下降，说明运行时开销、分配与同步开始占据主导。距离矩阵的多 GPU 扩展最弱，因为每个查询会产生大规模输出行；输出分配与全局内存流量主导了命令时间。这是一个有价值的负面结果：系统已可使用 8 张 GPU，但要让大规模输出 kernel 真正扩展，还需要 memory pool、持久输出缓冲区与 CUDA stream。

\section{盈亏平衡分析（Break-even Analysis）}
CUDA 加速存在\textbf{固定开销}（fixed overhead）。小规模工作负载可能在 CUDA 上更慢，因为 kernel 启动、同步、分配与命令分发会压过真正的计算量。图 \ref{fig:break-even} 以工作项数 \(N \times Q\) 为横轴、加速比为纵轴。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.82\textwidth]{geometry-break-even.png}
  \caption{按工作项统计的 CUDA 盈亏平衡分析}
  \label{fig:break-even}
\end{figure}

直接结论：只有工作项数量足以摊销启动、分配、同步与 UVec 管理成本后，CUDA 才真正值得使用。

保存的数据估计距离矩阵的盈亏平衡点约为 8192 个工作项，最近点约为 47848，圆形范围计数约为 121209。这些值并非普适常数，而依赖硬件、驱动状态、输入规模、输出规模，以及测量是否包含读回。

\section{消融与并发几何（Ablation and Concurrent Geometry）}
\textbf{消融实验}（ablation experiment）将内联字面量解析、预构建 UVec 句柄与读回开销分离。实验显示，预构建句柄可以显著降低命令延迟，尤其对 CUDA 命令更明显；将大规模输出转回脚本列表则可能支配总时间。因此，高性能 FTCL script 应尽量让数据在 UVec form 中跨多个 geometry commands 保持驻留，只在最后读取 summary results。

\begin{table}[H]
\centering
\caption{几何消融实验解释（Interpretation of geometry ablation experiment）}
\label{tab:ablation-interpretation}
\begin{tabularx}{\textwidth}{lX}
\toprule
实验模式 & 解释 \\
\midrule
内联字面量输入 & 测量从脚本文本解析点列表、创建临时数据结构、分发命令并执行后端的完整成本。该模式方便，但大输入时容易被脚本解析主导。 \\
预构建 UVec（无读回） & 测量句柄查找、设备同步、输出分配与后端执行，同时将数据保持在 UVec 形式中。该模式最接近高吞吐脚本流水线。 \\
预构建 UVec（含读回） & 额外加入将输出 UVec 转为脚本列表的成本。对于距离矩阵这类大规模输出，读回会主导总时间并掩盖 CUDA 计算收益。 \\
\bottomrule
\end{tabularx}
\end{table}

消融实验给出一条实践原则：FTCL 脚本应把 UVec 句柄当作长期存活的数据对象使用，避免将大规模中间结果转回字符串。这与 GPU 编程中的数据驻留（data residency）原则一致。加速效果最强的情况是：数据只复制到设备一次，随后由多个命令处理，最后再读回。

该解释也说明了为什么 CUDA 并不总是在小输入上更快。即使 kernel 本身很高效，整个命令路径仍须支付启动、同步、输出分配与可能读回的固定成本。因此，盈亏平衡点是系统级属性，而不仅是算法级属性。

\textbf{并发几何实验}如图 \ref{fig:concurrency-throughput} 与图 \ref{fig:concurrency-latency} 所示。CPU 吞吐量随 worker 数量增加而提升，直至出现竞争；CUDA 吞吐量更平缓，因为多个脚本 worker 会竞争同一张 GPU 与同步点。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{geometry-concurrency-throughput.png}
  \caption{并发几何工作负载的吞吐量}
  \label{fig:concurrency-throughput}
\end{figure}

直接结论：增加脚本 worker 数量只能在共享运行时或 GPU 资源成为瓶颈之前提升吞吐量。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{geometry-concurrency-latency.png}
  \caption{并发几何工作负载的 P95 延迟}
  \label{fig:concurrency-latency}
\end{figure}

直接结论：竞争会推高 P95 延迟，因此后续需要专用调度、内存池与基于 stream 的 CUDA 执行。

\section{小结}
本章从正确性、性能与可扩展性三个维度评估 FTCL。语义回归与 CPU/GPU 等价性测试证明系统具有可验证的正确性基础；单 GPU 与多 GPU 实验显示 CUDA 后端在大量独立工作项上具有明显优势；消融与并发实验则指出真正的瓶颈经常来自分配、读回、加锁与调度。

评估有意采用端到端口径。主要结论是：数据驻留、输出规模与运行时同步，往往决定脚本层异构系统的实际性能。
